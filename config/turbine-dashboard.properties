server.port=8089

turbine.aggregator.clusterConfig=reservation

turbine.clusterNameExpression=new String("default")
turbine.appConfig=reservation-service

eureka.instance.leaseRenewalIntervalInSeconds=10
eureka.client.registryFetchIntervalSeconds=5
eureka.serviceUrl.defaultZone=${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/

#eureka.client.serviceUrl.defaultZone=${vcap.services.${PREFIX:}eureka.credentials.uri:http://user:password@localhost:8761}/eureka/

#eureka.instance.metadata-map.management.port=${management.port:8081}